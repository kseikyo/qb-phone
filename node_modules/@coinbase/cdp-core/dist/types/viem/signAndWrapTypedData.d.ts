import { Hash } from 'viem';
import { EvmAddress, Hex, EIP712TypedData } from '../types.js';
export interface SignAndWrapTypedDataForSmartAccountOptions {
    smartAccount: EvmAddress;
    ownerAddress: EvmAddress;
    hash: Hash;
    chainId: number;
}
export interface SignAndWrapTypedDataForSmartAccountResult {
    signature: Hex;
}
export declare function signAndWrapTypedDataForSmartAccount(options: SignAndWrapTypedDataForSmartAccountOptions): Promise<SignAndWrapTypedDataForSmartAccountResult>;
export declare function createReplaySafeTypedData({ hash, chainId, smartAccountAddress, }: {
    hash: Hash;
    chainId: number;
    smartAccountAddress: EvmAddress;
}): EIP712TypedData;
export declare function createSmartAccountSignatureWrapper({ signatureHex }: {
    signatureHex: Hex;
}): Hex;
