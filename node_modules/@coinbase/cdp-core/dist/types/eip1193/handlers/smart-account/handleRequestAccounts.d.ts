import { EventEmitter } from '../../eventEmitter.js';
import { ProviderStoreInstance } from '../../types.js';
import { Address } from 'ox/Address';
export declare function handleRequestAccounts(eventEmitter: EventEmitter, store: ProviderStoreInstance): Promise<Address[]>;
