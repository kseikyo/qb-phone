import { SpendPermissionNetwork } from '@coinbase/cdp-api-client';
import { EvmAddress, Hex } from '../types.js';
export type RevokeSpendPermissionOptions = {
    evmSmartAccount: EvmAddress;
    network: SpendPermissionNetwork;
    permissionHash: string;
    useCdpPaymaster?: boolean;
    paymasterUrl?: string;
};
export type RevokeSpendPermissionResult = {
    userOperationHash: Hex;
};
export declare const revokeSpendPermission: (options: RevokeSpendPermissionOptions) => Promise<RevokeSpendPermissionResult>;
