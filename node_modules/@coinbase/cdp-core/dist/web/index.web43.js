import { ValidationError as n, InternalError as e } from "./index.web23.js";
function c(a, t, o) {
  if (!a)
    throw new n("User must be signed in");
  if (!t)
    throw new e("No auth state found");
  switch (o) {
    case "eoa":
      if (a.evmAccounts && a.evmAccounts.length > 0)
        throw new n("User already has an EVM EOA account");
      break;
    case "smart":
      if (a.evmSmartAccounts && a.evmSmartAccounts.length > 0)
        throw new n("User already has an EVM Smart Account");
      break;
    case "solana":
      if (a.solanaAccounts && a.solanaAccounts.length > 0)
        throw new n("User already has a Solana account");
      break;
  }
  return { user: a, authState: t };
}
export {
  c as validateUserCanCreateAccount
};
