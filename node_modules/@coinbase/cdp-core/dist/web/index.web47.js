import { InvalidAddressError as s } from "./index.web88.js";
import { isAddress as a } from "./index.web67.js";
function r(n) {
  if (typeof n == "string") {
    if (!a(n, { strict: !1 }))
      throw new s({ address: n });
    return {
      address: n,
      type: "json-rpc"
    };
  }
  if (!a(n.address, { strict: !1 }))
    throw new s({ address: n.address });
  return {
    address: n.address,
    nonceManager: n.nonceManager,
    sign: n.sign,
    signAuthorization: n.signAuthorization,
    signMessage: n.signMessage,
    signTransaction: n.signTransaction,
    signTypedData: n.signTypedData,
    source: "custom",
    type: "local"
  };
}
export {
  r as toAccount
};
