const a = (...o) => {
  const t = o.filter(Boolean);
  if (t.length === 0 || t.length === 1)
    return !0;
  let e;
  for (const s of t) {
    const r = Object.keys(s);
    if (!e || e.size === 0) {
      e = new Set(r);
      continue;
    }
    for (const n of r) {
      if (e.has(n))
        return !1;
      e.add(n);
    }
  }
  return !0;
};
export {
  a as default
};
