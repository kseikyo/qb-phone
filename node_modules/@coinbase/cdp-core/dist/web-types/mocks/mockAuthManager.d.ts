import { AuthState, IAuthManager } from '../auth/types.js';
import { OnAuthStateChangeFn, User } from '../types.js';
export declare class MockAuthManager implements IAuthManager {
    private authState;
    private authStateChangeCallback;
    constructor(_projectId: string);
    ensureInitialized(): Promise<void>;
    getUser(): User | null;
    isSignedIn(): Promise<boolean>;
    signOut(): Promise<void>;
    addAuthStateChangeCallback(callback: OnAuthStateChangeFn): void;
    getToken(): Promise<string | null>;
    getTokenExpiration(): Promise<number | null>;
    getWalletSecretId(): Promise<string>;
    getXWalletAuth(_options: {
        requestMethod: string;
        requestHost: string;
        requestPath: string;
        requestData?: Record<string, unknown>;
    }): Promise<string>;
    getAuthState(): AuthState | null;
    setAuthState(authState: AuthState): Promise<void>;
    clearAuthState(): Promise<void>;
    private refreshToken;
}
