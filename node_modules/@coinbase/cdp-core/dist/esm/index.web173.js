import { SizeExceedsPaddingSizeError as a } from "./index.web172.js";
function o(r, i = {}) {
  const { dir: n, size: e = 32 } = i;
  if (e === 0)
    return r;
  const t = r.replace("0x", "");
  if (t.length > e * 2)
    throw new a({
      size: Math.ceil(t.length / 2),
      targetSize: e,
      type: "Hex"
    });
  return `0x${t[n === "right" ? "padEnd" : "padStart"](e * 2, "0")}`;
}
export {
  o as pad
};
