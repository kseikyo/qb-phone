import { b as m } from "./index.native29.js";
import { configureCdpApiClient as O, setAuthManager as P, authenticateUserWithCustomAuth as b, initiateAuthentication as j, createEndUserEvmAccount as x, createEndUserEvmSmartAccount as v, createEndUserSolanaAccount as K, verifyEmailAuthentication as W, verifySmsAuthentication as H, verifyOAuthCode as R, signEvmHashWithEndUserAccount as B, signEvmTransactionWithEndUserAccount as D, signSolanaTransactionWithEndUserAccount as F, sendEvmTransactionWithEndUserAccount as N, sendSolanaTransactionWithEndUserAccount as J, signEvmMessageWithEndUserAccount as L, signSolanaMessageWithEndUserAccount as z, signEvmTypedDataWithEndUserAccount as $, sendUserOperationWithEndUserAccount as V, getUserOperationWithEndUserAccount as q, exportEndUserEvmAccount as G, exportEndUserSolanaAccount as Q } from "@coinbase/cdp-api-client";
import { Analytics as c } from "./index.native30.js";
import "./index.native31.js";
import { Keypair as k } from "@solana/web3.js";
import E from "bs58";
import { createAuthManager as X } from "./index.native32.js";
import { CUSTOM_AUTH_REFRESH_BUFFER_MS as Y } from "./index.native33.js";
import { OAuthManager as Z } from "./index.native34.js";
import { toAuthState as g } from "./index.native35.js";
import { withAuth as d } from "./index.native36.js";
import { MockAuthManager as ee } from "./index.native37.js";
import { MockOAuthManager as te } from "./index.native38.js";
import { mockUser as f } from "./index.native39.js";
import { getPlatformServices as p, getPlatformCrypto as _ } from "./index.native6.js";
import { isChainSupportedForCDPSends as ne } from "./index.native40.js";
import { getConfig as a, setCoreAuthManager as I, setCoreOAuthManager as S, getCoreAuthManager as o, getCoreOAuthManager as re, setConfig as se } from "./index.native28.js";
import { CustomAuthError as y } from "./index.native22.js";
import { serializeTransaction as U } from "./index.native41.js";
const _e = async (e) => {
  if (!e.projectId)
    throw new Error("Project ID is required");
  e.disableAnalytics === void 0 || e.disableAnalytics === !1 ? (c.identifier = e.projectId, c.isUsingMocks = e.useMock === !0, c.isProduction = !e.basePath || e.basePath === "https://api.cdp.coinbase.com/platform") : c.enabled = !1;
  let n;
  try {
    n = a().projectId !== e.projectId;
  } catch {
    n = !0;
  }
  if (se(e), a().useMock) {
    I(new ee(a().projectId)), S(new te());
    return;
  }
  let t;
  try {
    const r = p();
    t = r.secureStorage ? {
      getRefreshToken: () => r.secureStorage.getItem("cdp_refresh_token"),
      setRefreshToken: (s) => r.secureStorage.setItem("cdp_refresh_token", s),
      removeRefreshToken: () => r.secureStorage.removeItem("cdp_refresh_token")
    } : (
      // This is used only in WebKit browsers by virtue of the API
      {
        getRefreshToken: () => r.storage.getItem("cdp_refresh_token"),
        setRefreshToken: (s) => r.storage.setItem("cdp_refresh_token", s),
        removeRefreshToken: () => r.storage.removeItem("cdp_refresh_token")
      }
    );
  } catch {
    t = void 0;
  }
  if (O({
    debugging: a().debugging,
    basePath: a().basePath,
    refreshTokenStorage: t
  }), n) {
    const r = new Z();
    S(r);
    const s = X(a().projectId);
    I(s), P(s);
  }
  await o().ensureInitialized();
}, Ue = async (e) => (c.sendActionCallEvent({ name: "sign_in_with_email" }), A({
  email: e.email,
  type: "email"
})), Ce = async (e) => (c.sendActionCallEvent({ name: "sign_in_with_sms" }), A({
  phoneNumber: e.phoneNumber,
  type: "sms"
})), Me = async (e) => {
  c.sendActionCallEvent({ name: "sign_in_with_oauth" });
  const n = p().oauth.getRedirectUrl();
  if (a().useMock) {
    const r = new URL(n);
    r.searchParams.set("code", "mock-oauth-code"), r.searchParams.set("provider_type", e), r.searchParams.set("flow_id", "mock-flow-id"), p().oauth.initiateOAuthFlow(r.toString());
    return;
  }
  const { authUrl: t } = await A({
    redirectUrl: n,
    type: e
  });
  p().oauth.initiateOAuthFlow(t);
}, Te = async (e) => (c.sendActionCallEvent({ name: "verify_email_otp" }), w(
  e,
  "Mock email OTP verified",
  (n, t) => W(n, t)
)), Oe = async (e) => (c.sendActionCallEvent({ name: "verify_sms_otp" }), w(
  e,
  "Mock SMS OTP verified",
  (n, t) => H(n, t)
)), Pe = async (e) => (c.sendActionCallEvent({ name: "verify_oauth" }), w(
  e,
  "Mock OAuth code verified",
  (n, t) => R(n, t.providerType, t)
)), be = async () => {
  c.sendActionCallEvent({ name: "authenticate_with_jwt" });
  const e = a();
  if (!e.customAuth?.getJwt)
    throw new y(
      "customAuth.getJwt callback not configured. Please provide customAuth with getJwt in initialize() config."
    );
  let n;
  try {
    n = await e.customAuth.getJwt();
  } catch (u) {
    throw new y(
      u instanceof Error ? u.message : "Failed to get custom auth JWT"
    );
  }
  if (!n)
    throw new y("customAuth.getJwt returned null or undefined");
  if (e.useMock)
    return await o().setAuthState({
      accessToken: n,
      expiresAt: Date.now() + 1e3 * 60 * 60 * 24,
      user: f
    }), {
      message: "Mock JWT authentication successful",
      user: f,
      isNewUser: !1
    };
  const t = await b(a().projectId), r = new Date(Date.now() + Y).toISOString();
  let s = g(n, r, t.endUser);
  return await o().setAuthState(s), e.ethereum?.createOnLogin && (s = await C(
    s,
    n,
    r,
    e.ethereum.createOnLogin
  )), e.solana?.createOnLogin && (s = await M(s, n, r)), {
    message: "Authentication successful",
    user: o().getUser(),
    isNewUser: t.isNewEndUser
  };
}, je = async () => (c.sendActionCallEvent({ name: "get_current_user" }), o().getUser()), xe = async () => (c.sendActionCallEvent({ name: "is_signed_in" }), o().isSignedIn()), Ke = async () => {
  if (c.sendActionCallEvent({ name: "sign_out" }), a().useMock) {
    await o().signOut();
    return;
  }
  if (!await o().isSignedIn())
    throw new Error("User not signed in");
  await o().signOut();
}, We = async () => (c.sendActionCallEvent({ name: "get_access_token" }), o().getToken()), He = async () => (c.sendActionCallEvent({ name: "get_access_token_expiration" }), o().getTokenExpiration()), Re = (e) => {
  o().addAuthStateChangeCallback(e);
}, Be = (e) => {
  re().addOAuthStateChangeCallback(e);
}, De = async (e) => (c.sendActionCallEvent({ name: "sign_evm_hash" }), a().useMock ? { signature: "0x0" } : d(e, o(), async ({ user: n, walletSecretId: t }) => ({
  signature: (await B(a().projectId, n.userId, {
    hash: e.hash,
    address: e.evmAccount,
    walletSecretId: t
  })).signature
}))), Fe = async (e) => (c.sendActionCallEvent({ name: "sign_evm_transaction" }), a().useMock ? { signedTransaction: "0x0" } : d(e, o(), async ({ user: n, walletSecretId: t }) => {
  const r = U(e.transaction);
  return {
    signedTransaction: (await D(
      a().projectId,
      n.userId,
      {
        transaction: r,
        address: e.evmAccount,
        walletSecretId: t
      }
    )).signedTransaction
  };
})), Ne = async (e) => (c.sendActionCallEvent({ name: "sign_solana_transaction" }), a().useMock ? { signedTransaction: "mock-signed-transaction" } : d(e, o(), async ({ user: n, walletSecretId: t }) => ({
  signedTransaction: (await F(
    a().projectId,
    n.userId,
    {
      transaction: e.transaction,
      address: e.solanaAccount,
      walletSecretId: t
    }
  )).signedTransaction
}))), Je = async (e) => {
  if (c.sendActionCallEvent({ name: "send_evm_transaction" }), !ne(e.network))
    throw new Error(`Chain ${e.network} is not supported by the CDP Apis`);
  if (a().useMock)
    return { transactionHash: "0x0" };
  const n = U(e.transaction);
  return d(e, o(), async ({ user: t, walletSecretId: r }) => ({
    transactionHash: (await N(
      a().projectId,
      t.userId,
      {
        transaction: n,
        address: e.evmAccount,
        walletSecretId: r,
        network: e.network
      }
    )).transactionHash
  }));
}, Le = async (e) => (c.sendActionCallEvent({ name: "send_solana_transaction" }), a().useMock ? { transactionSignature: "mock-signature" } : d(e, o(), async ({ user: n, walletSecretId: t }) => ({
  transactionSignature: (await J(
    a().projectId,
    n.userId,
    {
      transaction: e.transaction,
      address: e.solanaAccount,
      walletSecretId: t,
      network: e.network
    }
  )).transactionSignature
}))), ze = async (e) => (c.sendActionCallEvent({ name: "sign_evm_message" }), a().useMock ? { signature: "0x0" } : d(e, o(), async ({ user: n, walletSecretId: t }) => ({
  signature: (await L(a().projectId, n.userId, {
    message: e.message,
    address: e.evmAccount,
    walletSecretId: t
  })).signature
}))), $e = async (e) => (c.sendActionCallEvent({ name: "sign_solana_message" }), a().useMock ? { signature: "mockSignature" } : d(e, o(), async ({ user: n, walletSecretId: t }) => ({
  signature: (await z(
    a().projectId,
    n.userId,
    {
      message: e.message,
      address: e.solanaAccount,
      walletSecretId: t
    }
  )).signature
}))), Ve = async (e) => (c.sendActionCallEvent({ name: "sign_evm_typed_data" }), a().useMock ? { signature: "0x0" } : d(e, o(), async ({ user: n, walletSecretId: t }) => ({
  signature: (await $(a().projectId, n.userId, {
    typedData: e.typedData,
    address: e.evmAccount,
    walletSecretId: t
  })).signature
}))), qe = async (e) => (c.sendActionCallEvent({ name: "send_user_operation" }), a().useMock ? {
  userOperationHash: "0x1234567890123456789012345678901234567890123456789012345678901234"
} : d(e, o(), async ({ user: n, walletSecretId: t }) => ({
  userOperationHash: (await V(
    a().projectId,
    n.userId,
    e.evmSmartAccount,
    {
      network: e.network,
      calls: e.calls.map((s) => ({
        to: s.to,
        value: String(s.value ?? 0n),
        data: s.data ?? "0x"
      })),
      walletSecretId: t,
      useCdpPaymaster: e.useCdpPaymaster ?? !1,
      paymasterUrl: e.paymasterUrl
    }
  )).userOpHash
}))), Ge = async (e) => (c.sendActionCallEvent({ name: "get_user_operation" }), a().useMock ? {
  userOpHash: e.userOperationHash,
  network: e.network,
  calls: [
    {
      to: "0x1234567890123456789012345678901234567890",
      value: "0",
      data: "0x"
    }
  ],
  status: "complete",
  transactionHash: "0x9876543210987654321098765432109876543210987654321098765432109876",
  receipts: []
} : d(e, o(), async ({ user: n }) => await q(
  a().projectId,
  n.userId,
  e.evmSmartAccount,
  e.userOperationHash
))), Qe = async (e) => {
  if (c.sendActionCallEvent({ name: "export_evm_account" }), a().useMock)
    return {
      privateKey: m.Buffer.from("mock-private-key").toString("hex")
    };
  const n = _(), t = await n.createExportKeyPair();
  return d(e, o(), async ({ user: r, walletSecretId: s }) => {
    const i = await G(a().projectId, r.userId, {
      address: e.evmAccount,
      walletSecretId: s,
      exportEncryptionKey: t.publicKeyBase64
    }), u = await n.decryptWithPrivateKey(
      t.privateKey,
      i.encryptedPrivateKey
    );
    return {
      privateKey: m.Buffer.from(u).toString("hex")
    };
  });
}, Xe = async (e) => {
  if (c.sendActionCallEvent({ name: "export_solana_account" }), a().useMock) {
    const r = m.Buffer.from("mock-private-key").subarray(0, 32), s = m.Buffer.alloc(32);
    r.copy(s);
    const i = k.fromSeed(s), u = m.Buffer.concat([i.secretKey.subarray(0, 32), i.publicKey.toBytes()]);
    return {
      privateKey: E.encode(u)
    };
  }
  const n = _(), t = await n.createExportKeyPair();
  return d(e, o(), async ({ user: r, walletSecretId: s }) => {
    const i = await Q(a().projectId, r.userId, {
      address: e.solanaAccount,
      walletSecretId: s,
      exportEncryptionKey: t.publicKeyBase64
    }), u = await n.decryptWithPrivateKey(
      t.privateKey,
      i.encryptedPrivateKey
    ), l = k.fromSeed(new Uint8Array(u)), h = m.Buffer.concat([l.secretKey.subarray(0, 32), l.publicKey.toBytes()]);
    return {
      privateKey: E.encode(h)
    };
  });
}, A = async (e) => {
  if (a().useMock)
    return "redirectUrl" in e ? {
      authUrl: "mock-auth-url",
      flowId: "mock-flow-id"
    } : {
      message: "Mock sign in initiated",
      flowId: "mock-flow-id"
    };
  if (await o().isSignedIn())
    throw new Error("User is already authenticated. Please sign out first.");
  const t = await j(a().projectId, e);
  return "authUrl" in t ? {
    flowId: t.flowId,
    authUrl: t.authUrl
  } : {
    flowId: t.flowId,
    message: "message" in t ? t.message : ""
  };
}, w = async (e, n, t) => {
  if (a().useMock)
    return await o().setAuthState({
      accessToken: "mock-access-token",
      expiresAt: Date.now() + 1e3 * 60 * 60 * 24,
      user: f
    }), {
      message: n,
      user: f,
      isNewUser: !1
    };
  const r = await t(a().projectId, e);
  let s = g(
    r.accessToken,
    r.validUntil,
    r.endUser
  );
  await o().setAuthState(s);
  const i = a();
  i.ethereum?.createOnLogin && (s = await C(
    s,
    r.accessToken,
    r.validUntil,
    i.ethereum.createOnLogin
  )), i.solana?.createOnLogin && (s = await M(
    s,
    r.accessToken,
    r.validUntil
  ));
  const u = o().getUser();
  return {
    message: r.message,
    user: u,
    isNewUser: r.isNewEndUser
  };
}, C = async (e, n, t, r) => {
  let s = e;
  if (!e.user.evmAccounts || e.user.evmAccounts.length === 0)
    try {
      const i = await o().getWalletSecretId();
      let u = await x(a().projectId, e.user.userId, {
        walletSecretId: i
      }), l = !1;
      const h = a().ethereum;
      if (h?.createOnLogin === "smart" && (l = h.enableSpendPermissions ?? !1), r === "smart") {
        const [T] = u.evmAccounts;
        u = await v(
          a().projectId,
          e.user.userId,
          {
            owner: T,
            enableSpendPermissions: l
          }
        );
      }
      s = g(n, t, u), await o().setAuthState(s);
    } catch (i) {
      throw new Error(`Failed to create EVM account: ${i}`);
    }
  if (r === "smart" && (!s.user.evmSmartAccounts || s.user.evmSmartAccounts.length === 0))
    try {
      const [i] = s.user.evmAccounts, u = await v(
        a().projectId,
        s.user.userId,
        {
          owner: i,
          enableSpendPermissions: !1
          // Defaulting to false until the feature is ready.
        }
      );
      s = g(n, t, u), await o().setAuthState(s);
    } catch (i) {
      throw new Error(`Failed to create EVM Smart Account: ${i}`);
    }
  return s;
}, M = async (e, n, t) => {
  let r = e;
  if (!e.user.solanaAccounts || e.user.solanaAccounts.length === 0)
    try {
      const s = await o().getWalletSecretId(), i = await K(
        a().projectId,
        e.user.userId,
        {
          walletSecretId: s
        }
      );
      r = g(n, t, i), await o().setAuthState(r);
    } catch (s) {
      throw new Error(`Failed to create Solana account: ${s}`);
    }
  return r;
};
export {
  be as authenticateWithJWT,
  Qe as exportEvmAccount,
  Xe as exportSolanaAccount,
  We as getAccessToken,
  He as getAccessTokenExpiration,
  je as getCurrentUser,
  Ge as getUserOperation,
  _e as initialize,
  xe as isSignedIn,
  Re as onAuthStateChange,
  Be as onOAuthStateChange,
  Je as sendEvmTransaction,
  Le as sendSolanaTransaction,
  qe as sendUserOperation,
  De as signEvmHash,
  ze as signEvmMessage,
  Fe as signEvmTransaction,
  Ve as signEvmTypedData,
  Ue as signInWithEmail,
  Me as signInWithOAuth,
  Ce as signInWithSms,
  Ke as signOut,
  $e as signSolanaMessage,
  Ne as signSolanaTransaction,
  Te as verifyEmailOTP,
  Pe as verifyOAuth,
  Oe as verifySmsOTP
};
